# vpnht-api

### Sample config.json
```
{
    "CREDENTIALS": {
        "API_KEY": "YOUR_KEY",
        "API_SECRET": "YOUR_SECRET"
    },
    "LOCAL_COMMAND": {
        "ADD": "ls -la",
        "DELETE": "ls -la",
        "UPDATE": "ls -la"
    }
}
```
