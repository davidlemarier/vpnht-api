# vpnht-api

### Sample config.json
```
{
    "CREDENTIALS": {
        "API_KEY": "",
        "API_SECRET": ""
    },
    "LOCAL_COMMAND": {
        "ADD": "ls -la",
        "DELETE": "ls -la",
        "PASSWORD": "ls -la",
        "RENEW": "ls -la"
    }
}
```
